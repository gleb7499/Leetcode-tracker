/**
 * Single source of truth for desktop split layout geometry.
 * Keep these classes in sync between App content reserve and SidePanel width.
 */
export const DESKTOP_SPLIT_PANEL_WIDTH_CLASS = "w-[min(50vw,860px)] min-w-[500px]"
export const DESKTOP_SPLIT_PANEL_RIGHT_OFFSET_CLASS = "right-24"
export const DESKTOP_SPLIT_CONTENT_RESERVE_CLASS = "lg:pr-[calc(min(50vw,860px)+6rem)]"